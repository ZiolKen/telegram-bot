# ZiolKen Telegram Bot

Telegram-first bot with utilities, moderation, economy, games and leveling.

## Required env

```env
TELEGRAM_BOT_TOKEN=your_bot_token
DATABASE_URL=postgres_url
OWNER_ID=your_telegram_user_id
PORT=3000
```

## Run

```bash
npm install
npm run migrate
npm start
```